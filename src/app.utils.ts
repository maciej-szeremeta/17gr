export const PASSWORD_RULES = /^(?=.*?[A-Z])(?=.*[a-z])(?=.*[0-9])(?=.*[#?!@$%^&*-]).{6,24}$/;

export const MSG = {
  PASSWORD_RULE_MSG:'Hasło powinno zawierać jedną dużą przynajmniej jedna mała liter oraz znak specjalny',
};